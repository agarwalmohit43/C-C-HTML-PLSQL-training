/*Program to demonstrate the return statement*/
#include "factorial.h"	/*Included Header file which contains the prototype of factorial function*/
#include <stdio.h>
void main()
{
	int limit;
	printf("Enter the factorial Number:");
	scanf("%d",&limit);
	printf("Factorial of %d is %d",n,factorial(limit));
}
